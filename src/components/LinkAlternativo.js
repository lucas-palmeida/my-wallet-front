import { Link } from "react-router-dom";
import styled from "styled-components";

const LinkAlternativo = styled(Link)`
font-family: 'Raleway', sans-serif;
font-size: 15px;
font-weight: 700;
line-height: 18px;
color: #FFFFFF;
`;

export default LinkAlternativo;